version: '3'
services:
  app:
    build: .
    ports:
      - "8081:8081"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8081/health | grep -q ok"]
      interval: 30s
      timeout: 10s
      retries: 3
